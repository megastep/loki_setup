#include "carbondebug.h"

void carbon_debug(const char *str)
{
    // For now, we'll just print to the console.  In the future this
    // might be something more sophisticated.
    printf(str);
}